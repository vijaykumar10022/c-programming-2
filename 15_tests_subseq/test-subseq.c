#include <stdio.h>
#include <stdlib.h>
size_t maxSeq(int * array, size_t n);

int main() {
    int array1[] = {1, 1, 1, 1}
    if (maxSeq(array1[], 4) != 1){
        return EXIT_FAILURE
    }
    return EXIT_SUCCESS
}
